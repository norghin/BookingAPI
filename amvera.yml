meta:
  environment: csharp
  toolchain:
    name: dotnet
    version: 6.0

build:
  image: mcr.microsoft.com/dotnet/sdk:6.0

run:
  image: mcr.microsoft.com/dotnet/aspnet:6.0
  buildFileName: bin/YachtCareAPI       # путь к бинарю без расширения
  persistenceMount: /data
  containerPort: 80